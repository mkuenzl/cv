version: "3.8"

services:
  app:
    container_name: dev-personal-app
    restart: unless-stopped
    image: dev-personal-app
    build:
      context: .
      target: development
    volumes:
      - ./src:/app/src
    ports:
      - 3000:3000
